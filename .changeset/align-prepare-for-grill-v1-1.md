---
"mattpocock-skills": patch
---

Align `/prepare-for-grill` with the v1.1 planning flow. Sources are now optional and explicitly blocked when they cannot be assessed, report-only behavior is unambiguous, and oversized prompts route to separate grills, `/wayfinder`, or `/to-spec` → `/to-tickets` according to their shape.
